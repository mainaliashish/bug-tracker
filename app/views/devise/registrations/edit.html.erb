<div class="row">
  <p class="lead text-success fw-bold fs-4">
    Updae your profile
  </p>
  <div class="col-6">
  <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put, multipart: true }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>
    <%= f.input :first_name, class: 'mb-2' %>
    <%= f.input :last_name, class: 'mb-2'  %>
    <%= f.input :email, class: 'mb-2'  %>
    <%= f.file_field :profile_photo, required: true, class: 'mb-2 mt-3' %>
    <%= f.association :role, selected: Role.first, class: 'mb-2' %>
    <%= f.input :password, class: 'mb-2 mt-2', hint: 'Leave blank if you do not want to change your password.'   %>
    <%= f.input :password_confirmation, class: 'mt-2', hint: 'Leave blank if you do not want to change your password.'   %>
    <%= f.input :current_password, class: 'mt-2', hint: 'Input your current password to proceed' %>
    <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
    <% end %>
    <%= f.button :submit, data: { turbo: false }, value: 'Update', class: 'btn btn-primary mt-2' %>

  <% end %>
  </div>
</div>

<p>Unhappy? 😥 <%= button_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete, class: "btn btn-danger" %></p>

<%= link_to "Back", :back %>
